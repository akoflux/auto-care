<Window x:Class="FleetManager.Views.UtilisateursWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fleet Manager - Gestion des Utilisateurs" Height="650" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1565C0">
            <TextBlock Text="Gestion des Utilisateurs"
                      FontSize="24"
                      FontWeight="Bold"
                      Foreground="White"
                      VerticalAlignment="Center"
                      Margin="20,0"/>
        </Border>

        <!-- Formulaire d'ajout -->
        <Border Grid.Row="1" Background="White" Margin="20,20,20,10" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Color="#CCC" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Ajouter / Modifier un utilisateur" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Nom" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtNom" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="Prenom" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtPrenom" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="Email" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtEmail" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                        <TextBlock Text="Mot de passe" FontSize="12" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="TxtPassword" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Role" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox x:Name="CmbRole" Height="35">
                            <ComboBoxItem Content="Super Admin" Tag="1"/>
                            <ComboBoxItem Content="Admin" Tag="2"/>
                            <ComboBoxItem Content="Employe" Tag="3"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button x:Name="BtnAnnuler" Content="Annuler" Width="100" Height="35" Margin="0,0,10,0" Click="BtnAnnuler_Click" Visibility="Collapsed"/>
                    <Button x:Name="BtnAjouter" Content="Ajouter" Width="100" Height="35" Background="#4CAF50" Foreground="White" Click="BtnAjouter_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Liste des utilisateurs -->
        <Border Grid.Row="2" Background="White" Margin="20,10,20,20" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Color="#CCC" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Liste des utilisateurs" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                <DataGrid Grid.Row="1" x:Name="DgUtilisateurs"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         SelectionMode="Single"
                         CanUserAddRows="False"
                         GridLinesVisibility="Horizontal"
                         BorderThickness="1"
                         BorderBrush="#DDD">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" Width="*"/>
                        <DataGridTextColumn Header="Prenom" Binding="{Binding Prenom}" Width="*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding RoleNom}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Modifier" Margin="0,0,5,0" Padding="8,4" Click="BtnModifier_Click" Tag="{Binding}"/>
                                        <Button Content="Supprimer" Padding="8,4" Background="#F44336" Foreground="White" Click="BtnSupprimer_Click" Tag="{Binding}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
