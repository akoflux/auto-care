<Window x:Class="FleetManager.Views.VehiculesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fleet Manager - Gestion des Vehicules" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1565C0">
            <TextBlock Text="Gestion des Vehicules"
                      FontSize="24"
                      FontWeight="Bold"
                      Foreground="White"
                      VerticalAlignment="Center"
                      Margin="20,0"/>
        </Border>

        <!-- Formulaire d'ajout -->
        <Border Grid.Row="1" Background="White" Margin="20,20,20,10" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Color="#CCC" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Ajouter / Modifier un vehicule" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Immatriculation" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtImmat" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="Marque" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtMarque" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="Modele" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtModele" Height="35" Padding="8"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                        <TextBlock Text="Carburant" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox x:Name="CmbCarburant" Height="35">
                            <ComboBoxItem Content="Essence"/>
                            <ComboBoxItem Content="Diesel"/>
                            <ComboBoxItem Content="Hybride"/>
                            <ComboBoxItem Content="Electrique"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Date d'achat" FontSize="12" Margin="0,0,0,5"/>
                        <DatePicker x:Name="DpDateAchat" Height="35"/>
                    </StackPanel>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button x:Name="BtnAnnuler" Content="Annuler" Width="100" Height="35" Margin="0,0,10,0" Click="BtnAnnuler_Click" Visibility="Collapsed"/>
                    <Button x:Name="BtnAjouter" Content="Ajouter" Width="100" Height="35" Background="#4CAF50" Foreground="White" Click="BtnAjouter_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Liste des vehicules -->
        <Border Grid.Row="2" Background="White" Margin="20,10,20,20" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Color="#CCC" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Liste des vehicules" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                <DataGrid Grid.Row="1" x:Name="DgVehicules"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         SelectionMode="Single"
                         CanUserAddRows="False"
                         GridLinesVisibility="Horizontal"
                         BorderThickness="1"
                         BorderBrush="#DDD">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Immatriculation" Binding="{Binding Immatriculation}" Width="*"/>
                        <DataGridTextColumn Header="Marque" Binding="{Binding Marque}" Width="*"/>
                        <DataGridTextColumn Header="Modele" Binding="{Binding Modele}" Width="*"/>
                        <DataGridTextColumn Header="Carburant" Binding="{Binding TypeCarburant}" Width="*"/>
                        <DataGridTextColumn Header="Date d'achat" Binding="{Binding DateAchat, StringFormat=dd/MM/yyyy}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Modifier" Margin="0,0,5,0" Padding="8,4" Click="BtnModifier_Click" Tag="{Binding}"/>
                                        <Button Content="Supprimer" Padding="8,4" Background="#F44336" Foreground="White" Click="BtnSupprimer_Click" Tag="{Binding}">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
